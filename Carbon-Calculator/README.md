# Carbon Calculator

This script uses CodeCarbon to capture energy & CO₂ metrics for baseline and app runs.

Usage examples:
- Baseline mode (idle): `./emission_calculation.sh --app-run false --team-name SmaranTeam`
- App mode (active): `./emission_calculation.sh --app-run true --team-name SmaranTeam --run-seconds 1800`


<!-- Make the script executable (in Git Bash):

cd Carbon-Calculator
chmod +x emission_calculation.sh
cd .. -->